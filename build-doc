#!/usr/bin/env nix-shell
#!nix-shell -i /bin/bash -p pandoc pandoc-plantuml-filter plantuml

find "$(dirname "$0")/docs" -name "*.md" -print0 | \
  sort -z | \
  xargs -0 -n 1 cat | \
  pandoc --metadata title="TDD Workshop" \
    --standalone \
    --filter pandoc-plantuml --from markdown "$@"
